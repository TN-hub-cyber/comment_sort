[tool.poetry]
name = "comment-sort"
version = "0.1.0"
description = ""
authors = ["soramame"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
streamlit = "^1.42.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
